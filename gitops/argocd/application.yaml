apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rhbk-keycloak
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/name: rhbk-keycloak
    app.kubernetes.io/part-of: quebec
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/masauve/qc-keycloak.git
    # Branche de test pour l'observabilité (§9). À repasser sur « main » après
    # fusion de la PR feat/observability-grafana-uwm.
    targetRevision: feat/observability-grafana-uwm
    path: gitops/overlays/production
  destination:
    server: https://kubernetes.default.svc
    namespace: keycloak
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  ignoreDifferences:
    - group: operators.coreos.com
      kind: Subscription
      jsonPointers:
        - /status
    - group: k8s.keycloak.org
      kind: Keycloak
      jsonPointers:
        - /status
    # volumeClaimTemplates est immuable apres creation et Kubernetes y ajoute
    # des defauts (volumeMode: Filesystem) : drift cosmetique permanent.
    - group: apps
      kind: StatefulSet
      name: keycloak-postgresql
      jsonPointers:
        - /spec/volumeClaimTemplates
    # Le controleur Tekton Triggers complete le spec de l'EventListener avec des
    # valeurs par defaut absentes du manifeste git (namespaceSelector: {} et,
    # dans le template du deploiement sink, container name: "" + metadata) :
    # drift cosmetique permanent.
    - group: triggers.tekton.dev
      kind: EventListener
      name: keycloak-github-webhook
      jsonPointers:
        - /spec/namespaceSelector
        - /spec/resources/kubernetesResource/spec/template
