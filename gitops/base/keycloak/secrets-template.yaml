# ATTENTION: Ce fichier est un GABARIT uniquement.
# Ne jamais committer des secrets en clair dans Git.
# Utiliser SealedSecrets (kubeseal) ou External Secrets Operator pour la gestion des secrets en production.
#
# Génération avec kubeseal:
#   kubectl create secret generic keycloak-db-secret \
#     --from-literal=username=keycloak \
#     --from-literal=password=VOTRE_MOT_DE_PASSE \
#     --dry-run=client -o yaml | \
#     kubeseal --controller-namespace sealed-secrets --format yaml > gitops/base/keycloak/sealed-db-secret.yaml
#
# Génération du secret TLS:
#   oc create secret tls keycloak-tls-secret \
#     --cert=tls.crt --key=tls.key -n keycloak
#
---
# Remplacer par un SealedSecret en production
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-db-secret
  namespace: keycloak
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
type: Opaque
stringData:
  username: keycloak
  password: REMPLACER_PAR_MOT_DE_PASSE_SECURISE
